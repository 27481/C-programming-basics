// print 3 numbers in ascending and descing order 

#include<stdio.h>
    int main(){
    int a, b, c;

    printf("Enter the 3 numbers:\n");
    scanf("%d%d%d", &a, &b, &c);

    if ( (b>=c)&&(a>=c))
    {
        if (b>=c)
        {
            printf("\n Descending order : %d %d %d", a,b,c);
            printf("\n Ascending order : %d %d %d", c,b,a);
        }
        else{
            printf("\n ")
        }
        
    }
    else{


    }
    else if(){

    }
    return 0;
}