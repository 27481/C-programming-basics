#include <stdio.h>
int main()
{
    int principal=100, rate=4, years=1;
    int simplleIntrest = (principal * rate * years)/100;
    printf("The Value of simple Intrest is %d", simplleIntrest);
    return 0;
}