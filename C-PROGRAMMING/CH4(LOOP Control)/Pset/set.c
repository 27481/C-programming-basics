// WAP toprint natural number form 10 to 20 if initial loop ocunter is initialize to 0


#include<stdio.h>
    int main(){
    int i = 0; 
    while(i<=20){
        if (i >= 10)
        {
    printf("The value of i is %d\n",i);
        }
        i++; // i = i + 1 ; increment 
    }
    return 0;
}