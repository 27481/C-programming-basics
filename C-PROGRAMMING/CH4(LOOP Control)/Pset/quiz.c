#include<stdio.h>
    int main(){
    int i = 5;
    printf("The value after i++ is %d\n", ++i);
    printf("The value after i is %d\n", i);
    return 0;
}

// i++ = pehle print fir increment 
// ++i = pehele increment fir print kare 
// +++ = don not exist 

